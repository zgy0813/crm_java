<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zgy.qshy.sys.role.mapper.RoleMapper">

    <insert id="insertBaseRole" parameterType="java.util.Map">
        insert into sys_user_role(id,role_id,user_id) values (#{cm.id},#{cm.roleId},#{cm.userId})
    </insert>
</mapper>
